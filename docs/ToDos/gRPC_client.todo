gRPC client: 
    ☐ Create FileRequest 
        request := &pb.FileRequest{
            // Add your request parameters here
        }
        
    ☐ Create a context timeout (say, 5 seconds): 
        ctx, cancel := context.WithTimeout(context.Background(), 5*time.Second)
        defer cancel()
    